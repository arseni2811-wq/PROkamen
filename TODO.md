# TODO

## Сейчас

- [x] Применить миграции 002–006 к рабочей БД в отдельное окно обслуживания
  - Контекст: код требует UNIQUE finance, `orders.version` и таблицу `order_idempotency`; рабочая БД автоматически не изменялась.
  - Файлы: `backend/migrations/002_unique_order_finances.sql`–`006_calculator_manual_material.sql`, `backend/scripts/adopt-baseline.js`, `backend/scripts/import-calculator-catalog.js`
  - Критерий готовности: сделан backup/restore test, legacy baseline принят явной opt-in командой, миграции применены, каталог импортирован и материалы получили подтверждённые цены/публичность.

- [ ] Проверить незакоммиченную нормализацию дедлайнов
  - Контекст: в рабочем дереве изменены автокоррекция двухзначного года и подготовка значения `input[type=date]`; результат не подтвержден тестом в браузере.
  - Файлы: `public/crm/crm/js/order.js`
  - Критерий готовности: создание, загрузка и изменение всех дат заказа корректны после перезагрузки; изменение осознанно сохранено или отменено владельцем.

- [x] Восстановить исполняемую миграцию Sprint 2
  - Контекст: после SQL в файле присутствует посторонний XML-подобный фрагмент.
  - Файлы: `backend/migrations/sprint2_tables.sql`, `backend/server.js`
  - Критерий готовности: файл содержит только валидный идемпотентный SQL и успешно выполняется на тестовой БД.

- [x] Зафиксировать полную схему чистой БД
  - Контекст: сервер умеет дополнять существующие таблицы, но в репозитории нет подтвержденного способа создать базовые таблицы с нуля.
  - Файлы: `backend/server.js`, `backend/migrations/`, `backend/controllers/`
  - Критерий готовности: новая пустая MySQL разворачивается одной документированной командой и сервер проходит `/api/health`.

## Далее

- [x] Закрыть объектную авторизацию заказов
  - Контекст: admin имеет полный доступ, manager — только к своим заказам, worker — к безопасной производственной проекции.
  - Критерий готовности: read/update/status/PDF/uploads/history и финансовые данные защищены серверной ownership-проверкой и integration tests.

- [x] Синхронизировать калькулятор, позиции и финансы
  - Контекст: schema v2 явно содержит несколько изделий, legacy остаётся single-item.
  - Критерий готовности: snapshot/order/items/finance/history обновляются одной транзакцией; legacy multi-item по-прежнему защищён `409`.

- [x] Добавить optimistic locking и идемпотентное создание заказа
  - Контекст: записи используют `orders.version`, POST принимает `Idempotency-Key` в области actor.
  - Критерий готовности: stale update и повтор ключа с другим payload возвращают `409`, одинаковый retry возвращает прежний заказ.

- [x] Синхронизировать технический справочник статусов
  - Контекст: `lead` используется при создании заказа и разрешен валидацией, но отсутствует в начальном списке статусов.
  - Файлы: `backend/utils/seedStatuses.js`, `backend/utils/stateMachine.js`, `backend/middleware/schemas.js`, `public/crm/crm/js/order.js`
  - Критерий готовности: один и тот же полный набор статусов используется схемой, БД, канбаном и карточкой заказа.

- [x] Подключить актуальные интеграционные тесты к npm
  - Контекст: `npm test` всегда завершается ошибкой, а отдельный сценарий содержит устаревшее ожидание строгих переходов статуса.
  - Файлы: `backend/package.json`, `backend/test_sprint1.js`, `backend/utils/stateMachine.js`
  - Критерий готовности: документированная npm-команда запускает воспроизводимые тесты и возвращает корректный код завершения.

- [x] Устранить встроенные учетные данные и резервный JWT-секрет из текущей версии
  - Контекст: значения зашиты в скрипт инициализации, PHP-админку и fallback серверной аутентификации.
  - Файлы: `backend/init-users.js`, `backend/server.js`, `backend/controllers/authController.js`, `backend/middleware/auth.js`, `public/php/admin_handler.php`
  - Критерий готовности: секреты поступают из защищенной конфигурации, отсутствуют в отслеживаемых файлах и описаны только как имена переменных.

## Улучшения

- [x] Убрать абсолютный localhost из URL вложений
  - Контекст: API формирует адрес вложения с фиксированным `http://localhost:3000`.
  - Файлы: `backend/routes/orders.routes.js`, `public/crm/crm/js/api.js`
  - Критерий готовности: вложения открываются локально и на развернутом домене без изменения кода.

- [ ] Снизить зависимость CRM от CDN
  - Контекст: Tailwind CSS и библиотека экспорта подключаются внешними скриптами непосредственно из HTML.
  - Файлы: `public/crm/crm/*.html`
  - Критерий готовности: выбранный способ поставки ресурсов документирован и проверен в среде демонстрации.

- [ ] Разделить старт сервера и миграции
  - Контекст: `server.js` содержит маршрутизацию, раздачу статики и большой блок изменения схемы БД.
  - Файлы: `backend/server.js`, `backend/migrations/`
  - Критерий готовности: миграции запускаются контролируемо, а обычный старт не изменяет схему неожиданно.

## Проверить

- [x] Проверить ролевую матрицу CRM и API
  - Контекст: роли определены, но защита на уровне API применяется не ко всем операциям заказа, а интерфейс использует отдельные клиентские проверки.
  - Файлы: `backend/routes/`, `backend/middleware/authorize.js`, `public/crm/crm/js/`
  - Критерий готовности: права администратора, менеджера и мастера сведены в таблицу и подтверждены тестами для каждого сценария.

- [x] Реализовать UX нескольких изделий калькулятора
  - Контекст: технический калькулятор добавляет несколько столешниц/подоконников, каждая со своей формой и деталями.
  - Файлы: `public/js/calculator-app.js`, `backend/controllers/ordersController.js`
  - Критерий готовности: schema v2 сохраняет несколько изделий и синхронизирует соответствующие `order_items`.

- [ ] Добавить готовую толщину изделия в калькулятор
  - Контекст: `edge_reinforced` означает кромку готовой толщиной более 40 мм и не связан с формой профиля `model_1`–`model_7`; текущая configuration не содержит `finishedThickness`.
  - Файлы: `public/js/calculator-app.js`, `backend/middleware/schemas.js`, `backend/services/calculatorService.js`
  - Критерий готовности: отдельное backward-compatible поле готовой толщины управляет выбором `edge_reinforced`, не выводя толщину из модели профиля.

- [ ] Уточнить физические размеры оставшихся строк QuartzBel
  - Контекст: после утверждённого mapping NORMAL/JUMBO dry-run №2 оставил 33 кварцевые строки без однозначных габаритов и толщины; одиночные числа из смещённых колонок не считаются размером.
  - Файлы: `MATERIAL_IMPORT_REPORT.md`, `backend/scripts/import-quartzbel-materials.js`
  - Критерий готовности: для каждого варианта известны реальные length/width/thickness, после чего dry-run не отклоняет строку по размеру.

- [ ] Утвердить тип активной цены и курс EUR для импорта материалов
  - Контекст: исходные ОПТ/ОПТ1/прочие цены и EUR/USD сохраняются раздельно; калькуляторная цена не должна выбираться или конвертироваться неявно.
  - Файлы: `backend/migrations/007_material_variants_and_prices.sql`, `backend/scripts/import-quartzbel-materials.js`
  - Критерий готовности: владелец подтвердил `--active-price-type`, курс `--eur-per-usd` и дату курса перед реальным импортом.

- [x] Провести визуальную QA калькулятора в доступном браузере
  - Контекст: проверены desktop и 375 px, смена формы, материала и опции с живым пересчётом; ошибок консоли и горизонтальной прокрутки нет.
  - Критерий готовности: desktop и 375 px, формы прямая/Г/П, фильтры материалов, сохранение заказа и публичная заявка проверены без ошибок консоли и горизонтальной прокрутки.

- [ ] Определить retention для idempotency keys
  - Контекст: завершённые ключи сохраняются без срока, чтобы retries не создавали дубликаты.
  - Файлы: `backend/migrations/004_order_idempotency.sql`, `backend/controllers/ordersController.js`
  - Критерий готовности: утверждены срок, безопасная процедура очистки и мониторинг объёма таблицы.

- [ ] Проверить совместимость PHP-формы с выбранным запуском
  - Контекст: Express блокирует `/php`, поэтому PHP-обработчики недоступны при запуске через `npm start`.
  - Файлы: `backend/server.js`, `public/php/`, `public/pages/contacts.html`
  - Критерий готовности: форма работает в целевой инфраструктуре либо неиспользуемый путь явно исключен из сценария демонстрации.

- [ ] Проверить загрузку и выдачу вложений вне localhost
  - Контекст: файлы сохраняются на диск, метаданные записываются в MySQL, а адрес ответа зависит от локального хоста.
  - Файлы: `backend/routes/orders.routes.js`, `backend/uploads/`, `public/crm/crm/js/order.js`
  - Критерий готовности: документ и фото загружаются, отображаются и доступны после перезапуска на целевой ОС.

- [ ] Проверить Git remote проекта
  - Контекст: текущий `origin` не совпадает с указанным для диплома репозиторием `arseni2811-wq/PROkamen`.
  - Файлы: `.git/config`
  - Критерий готовности: владелец подтвердил правильный удаленный репозиторий; локальная ветка связана с ним без потери истории.

## Перед защитой диплома

- [ ] Проверить запуск с чистого окружения
  - Контекст: нужны Node.js, npm, MySQL, переменные окружения, зависимости и полная схема БД.
  - Файлы: `backend/package.json`, `backend/package-lock.json`, `backend/.env`, `backend/migrations/`
  - Критерий готовности: проект развернут по документации на чистой машине без ручного исправления кода или БД.

- [ ] Пройти основные пользовательские сценарии
  - Контекст: демонстрация охватывает авторизацию, клиента, расчет, заказ, канбан, производство, вложения, PDF и архив.
  - Файлы: `public/crm/crm/`, `backend/routes/`, `backend/controllers/`
  - Критерий готовности: сценарии последовательно выполняются для нужных ролей на демонстрационных данных.

- [ ] Проверить обработку ошибок
  - Контекст: предусмотрены 400, 401, 403, 404 и 500, сетевые ошибки и глобальный обработчик Express.
  - Файлы: `backend/server.js`, `backend/middleware/`, `public/crm/crm/js/api.js`
  - Критерий готовности: неверный ввод, истекшая сессия, недоступная БД и отсутствующий ресурс дают понятное сообщение без повреждения данных.

- [ ] Проверить отсутствие критических ошибок в консоли
  - Контекст: в CRM сохранены диагностические `console.log`, `console.error` и alert при 401.
  - Файлы: `public/crm/crm/js/`, `backend/`
  - Критерий готовности: во время полной демонстрации нет необработанных исключений, повторных 401 и ошибок загрузки ресурсов.

- [ ] Проверить корректность интерфейса
  - Контекст: публичная часть использует собственные CSS, CRM — Tailwind CDN и адаптивные классы.
  - Файлы: `public/css/`, `public/*.html`, `public/pages/`, `public/crm/crm/`
  - Критерий готовности: ключевые экраны читаемы на демонстрационном разрешении, нет наложений, обрезки и сломанных изображений.

- [ ] Подготовить демонстрационные данные
  - Контекст: сценарии зависят от пользователей, справочников, клиентов, заказов, статусов и файлов.
  - Файлы: `backend/init-users.js`, `backend/utils/seedStatuses.js`, `public/assets/data/`
  - Критерий готовности: подготовлен обезличенный набор данных, покрывающий все показываемые этапы без ввода секретов на экране.

- [ ] Проверить документацию запуска
  - Контекст: текущие команды кратко зафиксированы, но создание чистой БД пока не определено.
  - Файлы: `AGENTS.md`, `CONTEXT.md`, `TODO.md`
  - Критерий готовности: другой человек запускает проект только по документации и получает рабочий публичный сайт и CRM.
